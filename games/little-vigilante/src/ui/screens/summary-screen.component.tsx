import { Box } from '@atoms/Box';
import { Card } from '@atoms/Card';

export const SummaryScreenComponent = () => {
  return (
    <Box css={{ p: '$3' }}>
      <Card css={{ p: '$3' }}>Thanks for playing</Card>
    </Box>
  );
};
