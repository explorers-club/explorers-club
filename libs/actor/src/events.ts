export const PLAYER_JOINED = (props: { actorId: string }) => props;
export const PLAYER_LEFT = (props: { actorId: string }) => props;
export const PLAYER_READY = (props: { actorId: string }) => props;
export const PLAYER_UNREADY = (props: { actorId: string }) => props;
