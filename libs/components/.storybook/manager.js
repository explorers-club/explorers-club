import { addons } from '@storybook/addons';

addons.setConfig({
  // Disable keyboard shortcuts in storybook because we use the keyboard for
  // various in-game controls
  enableShortcuts: false,
});
