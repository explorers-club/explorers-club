import { FC } from 'react';
import { Button } from '@atoms/Button';

export const ClaimableFooter: FC = () => {
  return (
    <Button fullWidth color="blue" size="3">
      Start Club
    </Button>
  );
};
