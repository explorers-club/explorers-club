// import { LobbyActor } from '@explorers-club/party';
import styled from 'styled-components';
import { Lobby } from './lobby.component';

export function PartyScreenComponent() {
  return (
    <Container>
      <Lobby />
    </Container>
  );
}

const Container = styled.div``;
